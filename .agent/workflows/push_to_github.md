---
description: Push changes to GitHub automatically
---

// turbo-all
1. Add all changes
   `git add .`

2. Commit changes
   `git commit -m "Auto-update: Applied user requested changes"`

3. Push to main branch
   `git push origin main`
